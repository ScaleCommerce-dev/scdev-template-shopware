# Full Shopware build: admin + storefront + theme + public bundle assets.
# Shortcut for:  scdev exec app shopware-cli project ci --with-dev-dependencies

[no-exit-message]
default:
    scdev exec app shopware-cli project ci --with-dev-dependencies
